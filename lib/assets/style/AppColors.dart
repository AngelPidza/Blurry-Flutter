import 'package:flutter/material.dart';

class AppColors {
  // Colores Primarios
  static const Color primaryColor = Color(0xFFd2cfbd);
  static const Color secondaryColor = Color(0xFF202020);

  // Colores de Fondo
  static const Color backgroundColor = Color(0xFF873132);
  static const Color cardColor = Color(0xFFe6413b);
  static const Color onlyColor = Color(0xFFfde61e);

  // Colores de Texto
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color.fromARGB(255, 255, 255, 255);

  // Colores de Errores
  static const Color errorColor = Color(0xFFB00020);
}
